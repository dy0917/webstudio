


            <div class="modal-body" style="display:none">

                <form id="fromRegister" role="form">

                    <div class="form-group">
                        <label for="exampleInputEmail1">Email address</label>
                        {{input type="email" value=registeremail class="form-control" placeholder="Enter email"}}
                    </div>
                    <div class="form-group">
                        <label for="exampleInputPassword1">Password</label>
                        {{input type="password" value=registerpassword1 class="form-control" placeholder="Password"}}
                        <label for="exampleInputPassword1">Please confirm your password</label>
                        {{input type="password" value=registerpassword2 class="form-control" placeholder="Password"}}
                    </div>

                    {{#if registerErrorMessage}}
                    <ul style="color:red">
                        {{#each item in registerErrorMessage}}
                        <li>
                            {{item}}
                        </li>
                        {{/each}}
                    </ul>
                    {{/if}}
                </form>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal"{{action "closewindow"}}>canel</button>
                    <button id="btnlogin" class="btn btn-primary ladda-button" data-style="expand-right" {{action "registerClick"}}><span class="ladda-label">Register</span><span class="ladda-spinner"></span><span class="ladda-spinner"></span></button>

                </div>

            </div>

